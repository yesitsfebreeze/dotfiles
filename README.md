1. Clone this repo to "~/.config/dotiles"
2. Install just: [github.com/casey/just](github.com/casey/just)  
3. Run: `just install`

Some files will have this line at the top:  
`os.execute('cd ~/.config/dotfiles && just update &')`

> those files will automatically check for updates when executed